#include <iostream>

int main() {
  int max[150]; int current[150]; 
  int num_zeros_in1=0; int num_zeros_in2=0;
  std::string data;
  std::cin>> data;
  int datalength = data.length();
  int num_layers = datalength/150;
  std::cout<< datalength % 150;
  //for (int i=0; i<num_layers; i++){


  //}
  //read stuff into array1, and count the zeros

  //while >>input:
  //read the next stuff into array2, count zeros
  //compare num zeros for both of them
  //if current>max, move current info into max
  //at the end do what they want
  return 0;
}
